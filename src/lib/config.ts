export const config = {
  // Feature flags to transition pages from mock data to Supabase database queries
  useLiveEvents: true,
  useLiveTournamentDetails: true,
  useLiveRegistration: true,
  useLiveAdmin: true,
  useLiveMatches: true,
}
